# 🚀 C++ Projects Repository

A growing collection of C++ projects exploring algorithms, data structures, and mathematical modeling. Each folder contains a self-contained project with source code and documentation.

## 📁 Projects Overview

| Project Folder       | Description                                                                 |
|----------------------|-----------------------------------------------------------------------------|
| `HanoiTower`         | Recursive solution to the classic Tower of Hanoi puzzle.                   |
| `Matrix Operations`  | Matrix class with support for arithmetic, inversion, rank, and BFS methods.|
| `Suduko`             | Sudoku solver using backtracking and constraint propagation.               |
| `bank_account`       | Simple banking system with account management and transaction logic.       |

## 🛠️ How to Compile & Run

Each project contains `.cpp` files and may include a `main.cpp`. To compile and run:

```bash
g++ -std=c++11 -o output main.cpp
./output
